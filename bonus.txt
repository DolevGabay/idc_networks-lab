our bonus implements:
The Bonus class provides additional functionality to the web server beyond serving regular HTTP requests.
It includes methods for displaying a bonus page with a form to delete parameters and for handling the deletion of parameters.
we comment "// bonus" every where in the code that is about the bonus. 